#include <iostream>
using namespace std;
int main()
{
    int a, b, c, year;
    cout<<"Enter the no: "<<"\n";
    cin>>a;
   //year= ();
    if(a%365==0)
    {
        cout<<"the days are year";
    }
   else
   {
    cout<<"the days are not a year";
   }
}
